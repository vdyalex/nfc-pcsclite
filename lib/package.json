{
  "name": "pcsclite",
  "version": "1.0.0",
  "private": true,
  "gypfile": true,
  "description": "Bindings over PC/SC to access Smart Cards",
  "main": "./index.js",
  "types": "./index.d.ts",
  "files": [
    "./src/*.h",
    "./src/*.cpp",
    "./binding.gyp"
  ],
  "scripts": {
    "postinstall": "node-gyp rebuild",
    "build": "node-gyp configure build",
    "test": "mocha --project tsconfig.json \"test/**/*.spec.js\" --exit"
  },
  "dependencies": {
    "node-addon-api": "*"
  },
  "devDependencies": {
    "chai": "^6.2.0",
    "mocha": "^11.7.4",
    "sinon": "^21.0.0"
  }
}
